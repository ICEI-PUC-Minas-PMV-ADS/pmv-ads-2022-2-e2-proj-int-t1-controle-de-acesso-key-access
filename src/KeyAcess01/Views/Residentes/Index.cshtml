@model IEnumerable<KeyAcess01.Models.Residente>

@{
    ViewData["Title"] = "Index";
}

<h2>Residentes</h2>

<p>
    <a class="btn btn-info" asp-action="Create">Adicionar Residente</a>
    <a class="btn btn-dark" asp-area="" asp-controller="Home" asp-action="Index">Voltar para a Home</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Apartamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cpf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Apartamento.NmrApartamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                 <a class="btn btn-dark" asp-action="Edit" asp-route-id="@item.Id">Editar</a> |
                 <a class="btn btn-secondary" asp-action="Details" asp-route-id="@item.Id">Detalhes</a> |
                 <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Remover</a>
            </td>
        </tr>
}
    </tbody>
</table>
